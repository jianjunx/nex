# Force static linking of OpenSSL to avoid runtime dependency on system libssl
# Fixes: "Library not loaded: /opt/homebrew/*/libssl.3.dylib"
[env]
OPENSSL_NO_VENDOR = { value = "0", force = true }
OPENSSL_STATIC = { value = "1", force = true }
